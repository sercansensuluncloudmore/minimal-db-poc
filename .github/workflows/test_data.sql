CREATE DATABASE test_database;

use test_database; 

CREATE TABLE Persons (PersonID int,Username varchar(255));
INSERT INTO Persons (PersonID, Username) VALUES (1, 'Test_Username'):
SELECT * FROM Persons